'use client'

// Stripe payment method embedding has been removed.

export const PaymentMethodForm = () => {
  return (
    <div className="p-4 text-sm text-gray-500">
      Payment method management via embedded form is not available. Payments are
      handled via crypto checkout.
    </div>
  )
}

export default PaymentMethodForm
