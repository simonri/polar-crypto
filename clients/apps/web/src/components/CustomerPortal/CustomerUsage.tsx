'use client'

import { Client } from '@polar-sh/client'

interface CustomerUsageProps {
  api: Client
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const CustomerUsage = (_props: CustomerUsageProps) => {
  return null
}
