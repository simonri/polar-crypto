export const useDisputes = (
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  _organizationId: string,
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  _parameters?: Record<string, unknown>,
) => ({ data: undefined, isLoading: false }) as const
